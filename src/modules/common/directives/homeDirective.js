'use strict';

module.exports = /*@ngInject*/
  function homeDirective(/* inject dependencies here, i.e. : $rootScope */) {
    return {
      link: function (scope, element) {
        // Do something awesome
      }
    };
  };
